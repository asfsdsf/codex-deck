interface:
  display_name: "AI Terminal"
  short_description: "Approval-first terminal planning with reactive tagged step cards"
  default_prompt: "Plan terminal work as markdown plus one <ai-terminal-plan> block containing ordered single-command steps, wait for approval before execution, adapt to structured execution feedback, and finish with <requirement_finished>...</requirement_finished> when the request is done."
